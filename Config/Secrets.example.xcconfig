// Copy to Config/Secrets.xcconfig and fill in. That file is git-ignored.
// Nothing here is read at build time unless the corresponding SDK is actually wired —
// see ADR 0005; stubs are the default and need no keys.

FIREBASE_CLIENT_ID =
REVENUECAT_API_KEY =
SENTRY_DSN =
ONESIGNAL_APP_ID =
POSTHOG_API_KEY =
